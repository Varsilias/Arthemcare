/* eslint-disable */
import { useEffect } from "react"

class Auth {

    async signin(username, password) {
        //send a request to the Login API with the provided data and 
        //return the currently authenticated user's data
    }

    async signout() {
        //send a request to the logout API and return a response
    }

}

export default new Auth